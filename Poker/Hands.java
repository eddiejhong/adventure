public class Hands {

    private ArrayList<string> _hands = {"high card", "pair", "two pair", "three of a kind", "straight", "flush", "full house", "four of a kind", "straight flush", "royal flush"};

    public getHandStrength() {
        sortHand();

        switch(true) {
            case: isRoyalFlush()
                return _hands[last]
                break;
        }
    }

    public hasPair(ArrayList<cards> hand) {

    }

    public hasTwoPair(ArrayList<cards> hand) {

    }
}
